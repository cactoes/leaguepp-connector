#ifndef __CONNECTOR_HPP__
#define __CONNECTOR_HPP__

namespace connector {
    void CreateConnection();


    // make manual requests

    // listen to events via websockets

}; // namespace connector

#endif // __CONNECTOR_HPP__